{
  "name": "resume-webpage",
  "version": "1.0.0",
  "description": "A simple resume-style webpage hosted on AWS S3.",
  "main": "src/index.html",
  "scripts": {
    "start": "live-server src",
    "build": "echo 'Build step can be added here'",
    "deploy": "aws s3 sync src/ s3://your-bucket-name --delete"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}